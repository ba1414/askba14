/**
 * Timetable Generator Page
 * Standalone page for the timetable scheduling tool
 */

import React from 'react';
import { TimetableApp } from './timetable';

export const TimetablePage: React.FC = () => {
  return <TimetableApp />;
};

export default TimetablePage;
